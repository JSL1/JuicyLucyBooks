const credentials = {
    username: import.meta.env.USERNAME,
    password: import.meta.env.PASSWORD,
    hostname: import.meta.env.HOST,
    port: import.meta.env.PORST,
    sid: import.meta.env.sid
};

export default connection;